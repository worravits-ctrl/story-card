# Railway-only deployment (no Supabase required)
# All data stored in browser localStorage

# No environment variables required!
# App works completely offline/client-side

# Optional: Set these for Railway deployment
PORT=3000
NODE_ENV=production