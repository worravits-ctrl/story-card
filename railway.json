{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "serve -s dist -l $PORT",
    "healthcheckPath": "/story-card/",
    "healthcheckTimeout": 100
  }
}